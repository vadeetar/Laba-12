[pytest]
testpaths = tests
python_files = test_*.py
# Игнорируем предупреждения от старой библиотеки passlib
filterwarnings =
    ignore::DeprecationWarning:passlib.*
    ignore::DeprecationWarning:crypt.*